# IMPORTANT: Environment Setup Instructions
# -------------------------------------
# 1. Create your own .env file based on this template
# 2. .env file is listed in .gitignore and should NEVER be committed to the repository
# 3. API keys and tokens should be shared securely person-to-person

# API URLs - Replace "exempel" with your local development server
VITE_API_URL=http://exempel:3000/api
VITE_SOCKET_URL=ws://exempel:3000

# Public keys (ONLY public keys, NEVER include private keys)
VITE_PUBLIC_TOKEN=xxxx_xxxx_xxxx  # Replace with your development token

# API Configuration exempels
VITE_API_TIMEOUT=5000             # 5 seconds timeout for API requests
VITE_MAX_UPLOAD_SIZE=5242880      # 5 MB maximum upload size

# Contact team lead for the correct values for your local development environment